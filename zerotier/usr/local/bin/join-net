#!/bin/sh

sysrc zerotier_enable=YES
service zerotier start

cnt=0

while [ $cnt -lt 100 ]; do
   if [ -f /var/db/zerotier-one/zerotier-one.port ]; then
       break;
   fi
   sleep 5
done

if [ ! -f /var/db/zerotier-one/zerotier-one.port ]; then
   exit 1
fi

zerotier-cli join ${ZEROTIER_NET}
